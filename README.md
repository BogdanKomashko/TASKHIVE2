# TaskHive - JIRA-подібний менеджер задач

Сучасний веб-додаток для управління задачами з дизайном, натхненним JIRA. Використовує Kanban-дошку для візуалізації та управління задачами.

## 🚀 Особливості

- ✅ Kanban-дошка з трьома колонками (To Do, In Progress, Done)
- 🎨 Сучасний дизайн, схожий на JIRA
- ➕ Створення, редагування та видалення задач
- 🔄 Зміна статусу задач через меню
- 💾 Зберігання даних в SQLite базі даних
- 📱 Адаптивний дизайн для мобільних пристроїв

## 📋 Вимоги

- Node.js (версія 14 або вище)
- npm або yarn

## 🛠️ Встановлення та запуск

### 1. Встановлення залежностей

#### Backend:
```bash
cd Backend
npm install
```

#### Frontend:
```bash
cd frontend
npm install
```

### 2. Запуск додатку

#### Запуск Backend (термінал 1):
```bash
cd Backend
npm run dev
```
Backend буде доступний на `http://localhost:3001`

#### Запуск Frontend (термінал 2):
```bash
cd frontend
npm run dev
```
Frontend буде доступний на `http://localhost:5173` (або інший порт, якщо 5173 зайнятий)

### 3. Відкрити в браузері

Відкрийте браузер і перейдіть за адресою, яку покаже Vite (зазвичай `http://localhost:5173`)

## 📁 Структура проєкту

```
TASKHIVE2/
├── Backend/
│   ├── index.js          # Express сервер з API
│   ├── package.json
│   └── tasks.db          # SQLite база даних (створюється автоматично)
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── KanbanBoard.jsx  # Головний компонент Kanban-дошки
│   │   │   └── TaskCard.jsx     # Компонент картки задачі
│   │   ├── main.jsx             # Точка входу React
│   │   └── styles.css           # Глобальні стилі
│   ├── index.html
│   └── package.json
└── README.md
```

## 🎯 Використання

1. **Створення задачі**: Натисніть кнопку "Створити задачу" в верхній частині екрану
2. **Редагування задачі**: Натисніть на іконку меню (⋮) на картці задачі та виберіть "Редагувати"
3. **Зміна статусу**: Використовуйте меню задачі для переміщення між колонками
4. **Видалення задачі**: Використовуйте меню задачі та виберіть "Видалити"

## 🔧 API Endpoints

- `GET /tasks` - Отримати всі задачі
- `POST /tasks` - Створити нову задачу
- `PUT /tasks/:id` - Оновити задачу
- `DELETE /tasks/:id` - Видалити задачу

## 🌐 Деплой

Для деплою на платформі типу Render, Vercel, або Heroku:

1. **Backend**: Налаштуйте змінну середовища `PORT` (за замовчуванням 3001)
2. **Frontend**: Оновіть `API_URL` в `KanbanBoard.jsx` на URL вашого backend сервера
3. **CORS**: Backend вже налаштований для роботи з CORS

## 📝 Примітки

- База даних SQLite створюється автоматично при першому запуску
- Всі дані зберігаються локально в файлі `tasks.db`
- Для продакшн використання рекомендується використовувати PostgreSQL або іншу базу даних

## 🎨 Технології

- **Frontend**: React, Vite, React Icons
- **Backend**: Node.js, Express, SQLite3
- **Стилі**: CSS3 з сучасними дизайн-патернами

---

Створено з ❤️ для ефективного управління задачами
